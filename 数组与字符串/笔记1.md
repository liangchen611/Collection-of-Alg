# 数组与字符串

## 0.python的print换行：
## 示例问题
**斐波那契数列：**
```python
def fibonacci(n):
    dp = [0] * (n + 1)
    dp[1] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]
    return dp[n]
    
    对于print函数，在Python中，结尾有一个默认但不直接显示的赋值，end="\n"，即自动换行。
    正常的Print语句，例如print("Hello.")，实际上相当于print("Hello.",end="\n").
    如果不希望在print之后执行换行，可以通过print("Hello.", end="")的方式规制结尾符，则不会换行。

1.大写字母的ASCII码：65~91；
   小写字母的ASCII码在此基础上+32；数字0~9的ASCII码，48~57

2.长度为N的0数组的定义:
    [0]*N，全部为字符型的数组:["0"]*N，空数组:[]*N

3.数组的切片：
    [a:b:c]，其中a与b为切片的下标索引，c为切片的步长，a为起点下标，b为终点下标前一个位置，因为python的切片是半开区间规则
切片c表示切片数组中两个元素在原数组中对应的下标的差值，例如S[0:5:2]，起点下标为0，终点下标为4，步长为2，从0开始数，经过2个单位长度取一个元素，0-1-2
，最终取得元素S[0]，S[2]，S[4]
切片会自动处理越界溢出的情况：返回相应的空数组，不会有index error

TIPS：其他应用了半开区间规则的情况：
    range函数：range(A,B)，生成一个序列，作用的范围是A~B-1，不包括终止位置。

    zip函数：zip(A,B)，将多个可迭代对象，如列表、元组等，打包为一个迭代器。每次迭代返回一个元组，包含来自各个可迭代对象的元素。若可迭代对象的长度
不一致，则zip操作会停在最短可迭代对象的终点处。
    a=[1,2,3]
    b=["a","b","c","d"]
    zip(a,b)=[(1,"a"),(2,"b"),(3,"c")]

    字典相关的函数：dict.items()，dict.keys()，dict.values

4.-1在python数组中表示倒数最后一个位置的索引
    同理，-2，-3等也可以表示倒数第二，倒数第三个元素的位置的索引。，步长为负数的情况下，会从右往左地遍历元素，但要求起始元素的下标必须比终止元素大，
    例如[4:0:-1]，如果是[2:3:-1]，则会报错，因为不能从前面的索引再往前搜索而得到后面的索引。

5.对于集合，字典等这样的可迭代对象，不能通过用下标进行索引的方法来对元素进行访问，因为它们之中的元素没有顺序。

6. enumerate迭代函数：返回一个索引和元素对的迭代器，可以用来同时访问列表中的索引和值。
    s = ['a', 'b', 'c']
    for idx, val in enumerate(s):
        print(idx, val)
    返回结果：
    0 a
    1 b
    2 c
